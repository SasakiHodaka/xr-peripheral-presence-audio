# Progress Summary

## 2026-07-06

### Done

- Collected a representative Unity run with participant response events across
  the three main conditions.
- Saved the run under `Runs/run_20260706_230957`.
- Generated run-local summaries:
  - `Runs/run_20260706_230957/summary.md`
  - `Runs/run_20260706_230957/event_summary.csv`
  - `Runs/run_20260706_230957/token_summary.csv`
  - `Runs/run_20260706_230957/token_summary_by_speaker.csv`
- Updated the scene validator to treat the current three-condition study design
  as valid:
  - `TRADITIONAL`
  - `DIRECTION_DISTANCE`
  - `FULL_SCENE_TOKEN`
- Added a response timing cue to the debug HUD:
  - `RESPOND NOW` when there is one clear active speaker
  - `WAIT` when responses would be ambiguous
- Added response-window event logging:
  - `response_window_start`
  - `response_window_end`

### Verified

- Latest response run:
  - direction responses: 11
  - speaker responses: 11
  - scored direction responses: 8
  - scored speaker responses: 7
- All three main conditions appeared in the response log.
- Token logs contained speaking rows, semantic rows, turn-holder rows, overlap
  rows, and all scripted semantic labels.

### Current Limitation

- The run is suitable as a prototype validation log, but not as a formal user
  study sample.
- Several responses were ambiguous because they were made during no-speaker or
  overlap moments.
- The next formal pilot should use clearer response timing instructions or a
  prompted response window.
- The HUD response timing cue and response-window events still need one Unity
  validation run.

### Next

- Validate the scene again after the validator update.
- Run a short session using only the `RESPOND NOW` cue and confirm that
  `ambiguousResponses` decreases.
- Use `Runs/run_20260706_230957/summary.md` as the current representative
  prototype result in the weekly report.

## 2026-07-02

### Done

- Re-ran the analysis tools against `Runs/latest_run`.
- Regenerated `summary.md`, `token_summary.csv`, and
  `token_summary_by_speaker.csv`.
- Fixed the weekly-report draft generated by
  `Tools/summarize_experiment_run.py` so it no longer preserves the broken
  mojibake template.
- Rewrote `docs/EVALUATION_HYPOTHESES.md` around the three main study
  conditions.
- Added `docs/SCENE_TOKEN_ONE_SLIDE.md` for a compact research-gap explanation.
- Fixed mojibake in the research overview title and Scene Token Japanese
  definition.

### Verified

- `python -m py_compile` passed for the analysis scripts.
- No mojibake patterns were found in `docs`, `README.md`, or `summary.md`.
- Token logs contain all three main conditions:
  `TRADITIONAL`, `DIRECTION_DISTANCE`, and `FULL_SCENE_TOKEN`.
- Scripted semantic labels are present in the token log.
- Current representative run has `4773` token rows.

### Current Limitation

- `Runs/latest_run` does not contain participant response events:
  `response_direction=0` and `response_speaker=0`.
- A new Unity Editor run is still required before response accuracy and
  latency can be evaluated.

### Next

- Open `Assets/Scenes/SceneTokenMock.unity`.
- Start a session with `Space` or the HUD button.
- For each main condition, record at least one direction response and one
  speaker response while the session is running.
- Re-run:

```bash
python Tools/check_latest_response_run.py Runs/latest_run
python Tools/analyze_event_logs.py Runs/latest_run event_summary.csv
python Tools/summarize_experiment_run.py Runs/latest_run summary.md
```

## 2026-06-25

## Done

- Scene token mock scene is regenerated and validated.
- Session-gated logging is enabled.
- HUD now has participant/session fields and session controls.
- Token and metrics log analyzers are available.
- Scene validation can run from Unity menu or batch mode.

## Verified

- Unity batch validation passed.
- Compilation errors were not found in the latest validation logs.

## Next

- Run one real session by pressing `Space` or `Start Session` before collecting logs.
